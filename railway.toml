[build]
builder = "NIXPACKS"
buildCommand = "npm install && pip install -U yt-dlp"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[nixpacks]
pkgs = ["python3", "ffmpeg", "pip"]
